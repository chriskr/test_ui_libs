{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "/test_ui_libs/react/build/",
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-scripts": "1.0.17",
    "uldu": "^1.0.0"
  },
  "scripts": {
    "setup": "copyfiles --up 1 -e ../common/*.config.js  ../common/*.* src",
    "start": "npm run setup && react-scripts start",
    "build": "npm run setup && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "webpack": "^3.8.1"
  }
}
